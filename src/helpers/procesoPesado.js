

export const procesoPesado = (iteration) => {
    
        for(let i = 0; i < iteration; i++) {
            console.log('Ahi vamos.......');
        }

        return `${iteration} iteraciones realizadas.`;
    
}
